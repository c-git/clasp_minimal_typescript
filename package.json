{
  "name": "app-script-project-title",
  "version": "0.0.0",
  "description": "",
  "main": "build/index.js",
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "clean": "rm -rf build && rm -rf dist",
    "bundle": "rollup --no-treeshake -c rollup.config.mjs",
    "build": "npm run clean && npm run bundle && cp appsscript.json dist/appsscript.json",
    "deploy": "npm run build && clasp push"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@google/clasp": "^3.3.0",
    "@types/google-apps-script": "^2.0.8",
    "rollup": "^4.60.3",
    "rollup-plugin-cleanup": "^3.2.1",
    "rollup-plugin-typescript2": "^0.37.0",
    "typescript": "^5.9.3"
  }
}